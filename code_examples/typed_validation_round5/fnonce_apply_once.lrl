(def once_id (pi #[once] x Nat Nat)
  (lam #[once] x Nat x))

(def apply_once
  (pi #[fn] f (pi #[once] x Nat Nat)
    (pi #[once] n Nat Nat))
  (lam #[fn] f (pi #[once] x Nat Nat)
    (lam #[once] n Nat
      (f n))))

(def entry Nat
  (print_nat (apply_once once_id (succ (succ zero)))))
