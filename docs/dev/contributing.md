# Contributing to LeanRustLisp

## Getting Started

1. Fork the repo.
2. `cargo test` to ensure everything is working.
3. Pick an issue or start a discussion.

## Pull Requests

- Keep PRs focused.
- Add tests for new features (`tests/*.lrl`).
- Run `cargo fmt` and `cargo clippy`.
