# Release Process

1. Update version in `Cargo.toml`.
2. Update `docs/reference/versioning.md` if language version changes.
3. Run full test suite.
4. Tag release on GitHub.
5. Publish to crates.io (future).
