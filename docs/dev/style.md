# Style Guide

## Rust Code
- Follow standard Rust formatting (`rustfmt`).
- Use `clippy` to catch common issues.

## LRL Code
- Lisp-style indentation (usually 2 spaces).
- `kebab-case` for identifiers.
- `CamelCase` for Types/Constructors.
