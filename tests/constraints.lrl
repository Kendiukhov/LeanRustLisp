;; Constraint/inference smoke under current syntax.
;; Keep this monomorphic because standalone implicit argument elaboration
;; depends on frontend feature gates.

(def id_nat
  (pi x Nat Nat)
  (lam x Nat x))

(def inferred_nat Nat
  (id_nat zero))

(add inferred_nat (succ zero))
