;; Indexing surface syntax smoke tests for VecDyn/Slice/Array.

(unsafe index_vec_dyn_ok (pi v (VecDyn Nat) Nat)
  (lam v (VecDyn Nat) v[zero]))

(unsafe index_slice_ok (pi s (Slice Nat) Nat)
  (lam s (Slice Nat) s[zero]))

(unsafe index_array_ok (pi a (Array Nat) Nat)
  (lam a (Array Nat) a[zero]))

(def entry Nat zero)
