
;; List Test: Create a list [1] and extract head
;; Using (app (lam l Ty body) val) because let is not supported in frontend yet
  (lam l (List Nat) 
    (match_list l Nat Nat
      (case (nil T) zero) 
      (case (cons T h t ih) h)
    )
  )
  (cons Nat (succ zero) (nil Nat))
)
