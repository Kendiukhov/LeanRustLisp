;; List Test: Create a list [1] and extract head
;; Parameters are NOT part of the match pattern - they're inferred from the scrutinee type
(app
  (lam l (List Nat)
    (match l Nat
      (case (nil) zero)
      (case (cons h t ih) h)
    )
  )
  (cons Nat (succ zero) (nil Nat))
)
