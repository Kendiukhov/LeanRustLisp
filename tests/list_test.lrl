;; List test: create [1] and extract head.

(def head_or_zero (pi l (List Nat) Nat)
  (lam l (List Nat)
    (match l Nat
      (case (nil) zero)
      (case (cons h t ih) h))))

(head_or_zero (cons (succ zero) nil))
