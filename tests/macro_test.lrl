;; Basic quote/quasiquote smoke.

(def q1 Nat (succ zero))

(def x Nat (succ zero))
(defmacro with_one (val)
  (quasiquote (add (succ zero) (unquote val))))
(def q2 Nat (with_one x))

(defmacro my_const (val)
  (quasiquote (unquote val)))

(add q1 (add q2 (my_const (succ (succ zero)))))
