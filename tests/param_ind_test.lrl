;; Parameterized inductive smoke (List Nat).

(def len (pi l (List Nat) Nat)
  (lam l (List Nat)
    (match l Nat
      (case (nil) zero)
      (case (cons h t ih) (succ ih)))))

(len (cons (succ zero) (cons zero nil)))
