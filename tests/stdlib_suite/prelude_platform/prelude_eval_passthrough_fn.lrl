;; prelude eval API smoke that works for both dynamic and typed impls.

(unsafe entry (pi #[once] code Dyn (pi #[once] cap EvalCap Dyn))
  (lam #[once] code Dyn
    (lam #[once] cap EvalCap
      (eval code cap))))
